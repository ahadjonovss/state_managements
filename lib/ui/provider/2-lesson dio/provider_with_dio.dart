import 'package:flutter/material.dart';

class ProviderWithDio extends StatelessWidget {
  const ProviderWithDio({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text("Provider with Dio"),
      ),
    );
  }
}
