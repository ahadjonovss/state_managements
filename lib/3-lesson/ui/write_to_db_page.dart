import 'package:flutter/material.dart';

class WriteToDbPage extends StatelessWidget {
  const WriteToDbPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Write to Db"),
      ),
    );
  }
}
